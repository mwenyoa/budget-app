<header class="p-4">
    <div class="container">
        <div class="d-flex flex-wrap align-items-between justify-content-between  nav-menu">
    

            <ul class="nav col-12 col-lg-auto me-lg-auto py-0 d-flex justify-content-between mb-md-0">
                <a href="/" class="d-flex justify-content-start   text-white text-decoration-none">
                    <h2><i class="fa-solid fa-coins"></i>  Coin Plan</h2>
                </a>
                 <% if user_signed_in? %>
                <li class="log-out mx-5 d-flex justify-content-end">
                    <%= button_to "Log Out", destroy_user_session_path, method: :delete, data: { turbo: false }, class:"btn btn-light ml-5" %>
                </li>  
                <% else %> <% end %>
            </ul>
           
        </div>
    </div>
  </header>